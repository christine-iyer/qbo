// API Configuration
const config = {
  // Change this to your production API URL when deploying
  API_BASE_URL: process.env.REACT_APP_API_URL || 'https://franky-app-ix96j.ondigitalocean.app/api',
};

export default config;
